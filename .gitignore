# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# JSON endpoint samples (contain real data)
*.json
# But allow REST describe metadata (no real data, just schema)
!*Describe.json
# Allow sample data folder (mock/demo data only, no real PII)
!sample-data/
!sample-data/*.json

# Data insert scripts (contain sample/legacy data)
*_data.sql

# Images
images/

# MCP config (contains local paths)
.mcp.json

# Oracle wallets (never commit)
wallets/
*.wallet/
